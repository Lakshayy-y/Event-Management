#include<stdio.h>
int main()
{
    int x;
    printf(" enter x");
    scanf("%d",&x);
    if(x%2==0)
    {
        printf("the no. is even");
    }
    else
    {  
        printf("the no. is odd");
    }
}